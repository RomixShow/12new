# 🔐 Инструкция по входу в админ-панель

## Доступ к админке

**URL:** https://rusintl-hub.preview.emergentagent.com/admin

⚠️ **Теперь админка защищена паролем!**

## Данные для входа:

```
Username: admin
Password: admin123
```

## Как войти:

1. Откройте https://rusintl-hub.preview.emergentagent.com/login
2. Введите username: **admin**
3. Введите password: **admin123**
4. Нажмите кнопку "Login"
5. Вы будете перенаправлены в админ-панель

## Безопасность:

✅ **JWT токены** - сессии с автоматическим истечением (24 часа)
✅ **Bcrypt хеширование** - пароли хранятся безопасно
✅ **Защищенные API** - все admin endpoints требуют авторизации
✅ **Автоматический logout** - при истечении токена

## Важно:

### ⚠️ ИЗМЕНИТЕ ПАРОЛЬ!

После первого входа создайте нового пользователя с безопасным паролем:

```bash
# Зайдите в backend
cd /app/backend

# Создайте нового пользователя
curl -X POST "https://rusintl-hub.preview.emergentagent.com/api/auth/create-user" \
  -H "Content-Type: application/json" \
  -d '{"username": "your_username", "password": "your_secure_password"}'
```

Или используйте Python скрипт:

```python
import asyncio
from motor.motor_asyncio import AsyncIOMotorClient
import os
from dotenv import load_dotenv
import bcrypt
import uuid
from datetime import datetime, timezone

load_dotenv('/app/backend/.env')

async def create_user(username, password):
    client = AsyncIOMotorClient(os.environ['MONGO_URL'])
    db = client[os.environ['DB_NAME']]
    
    password_hash = bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt()).decode('utf-8')
    
    user = {
        "id": str(uuid.uuid4()),
        "username": username,
        "password_hash": password_hash,
        "created_at": datetime.now(timezone.utc).isoformat()
    }
    
    await db.users.insert_one(user)
    print(f"✅ User {username} created!")
    client.close()

# Использование:
asyncio.run(create_user("your_username", "your_secure_password"))
```

### Удалить дефолтного admin:

```python
# В MongoDB
db.users.deleteOne({"username": "admin"})
```

## Выход из админки:

1. Нажмите кнопку "Выйти" в правом верхнем углу
2. Вы будете перенаправлены на страницу логина
3. Токен будет удален из браузера

## Что защищено:

✅ Страница `/admin` - требует авторизации
✅ Все API endpoints `/api/admin/*` - требуют JWT токен
✅ Создание, редактирование, удаление контента

## Что НЕ защищено:

✅ Публичные страницы сайта
✅ API для чтения контента (`/api/services`, `/api/cases` и т.д.)
✅ Контактная форма

## Технические детали:

- **JWT токены** с истечением через 24 часа
- **Токен хранится** в localStorage браузера
- **Автоматический redirect** на /login если токен отсутствует или истек
- **Bcrypt** для хеширования паролей (стоимость 12 раундов)
- **Bearer Authentication** для API запросов

## Troubleshooting:

**Проблема:** "Invalid token" или "Token expired"
**Решение:** Выйдите и войдите снова

**Проблема:** Забыли пароль
**Решение:** Создайте нового пользователя через Python скрипт выше

**Проблема:** Не могу зайти в админку
**Решение:** 
1. Проверьте что используете правильные credentials
2. Очистите localStorage браузера
3. Проверьте что backend запущен

## На продакшене:

### Обязательно сделайте:

1. **Измените SECRET_KEY** в backend/.env:
   ```
   JWT_SECRET_KEY=your-very-long-random-secret-key-here
   ```

2. **Удалите endpoint** `/api/auth/create-user` или добавьте к нему защиту

3. **Создайте безопасного пользователя** вместо admin/admin123

4. **Включите HTTPS** (обязательно для JWT)

5. **Настройте CORS** правильно в server.py

6. **Добавьте rate limiting** для endpoint логина

7. **Включите логирование** попыток входа

## Резюме:

✅ Админка защищена логином и паролем
✅ JWT токены для безопасных сессий  
✅ Bcrypt хеширование паролей
✅ Автоматический выход при истечении токена
✅ Все admin API защищены

**Дефолтные credentials:** admin / admin123
**⚠️ ОБЯЗАТЕЛЬНО измените на продакшене!**
