# 🌐 Настройка перевода на хостинге

## Текущая конфигурация (Бесплатная)

**Используется:** Google Translate через `deep-translator`
**API ключи:** НЕ требуются ✅
**Стоимость:** Бесплатно ✅

### Что уже сделано:
1. ✅ Библиотека `deep-translator` установлена в `requirements.txt`
2. ✅ Переводы кешируются в MongoDB (поля *_en)
3. ✅ После первого перевода используется кеш (быстро и без запросов)

---

## При деплое на хостинг

### Шаг 1: Убедитесь что установлены зависимости
```bash
pip install -r requirements.txt
```

### Шаг 2: Проверьте что backend имеет доступ к интернету
- Google Translate работает через веб-запросы
- Большинство хостингов (AWS, DigitalOcean, Heroku) это поддерживают

### Шаг 3: Всё! 🎉
Ничего больше не нужно. Переводы будут работать автоматически.

---

## Если нужен более надежный перевод (опционально)

### Вариант 1: Google Cloud Translation API (Платный, но надежнее)

**Стоимость:** $20 за 1 млн символов
**Преимущества:**
- Нет rate limiting
- Более стабильный
- Официальная поддержка

**Как подключить:**

1. Получите API ключ на https://cloud.google.com/translate
2. Установите библиотеку:
```bash
pip install google-cloud-translate
```

3. Обновите код в `/app/backend/server.py`:
```python
from google.cloud import translate_v2 as translate

translate_client = translate.Client()

def auto_translate(text: str, source_lang: str = 'ru', target_lang: str = 'en') -> str:
    try:
        result = translate_client.translate(text, target_language=target_lang, source_language=source_lang)
        return result['translatedText']
    except Exception as e:
        logging.error(f"Translation error: {e}")
        return text
```

4. Добавьте в `.env`:
```
GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json
```

---

### Вариант 2: DeepL API (Лучшее качество перевода)

**Стоимость:** Бесплатно до 500,000 символов/месяц
**Преимущества:**
- Лучшее качество перевода
- Бесплатный план

**Как подключить:**

1. Получите API ключ на https://www.deepl.com/pro-api
2. Замените в `server.py`:
```python
from deep_translator import DeeplTranslator

def auto_translate(text: str, source_lang: str = 'ru', target_lang: str = 'en') -> str:
    try:
        translator = DeeplTranslator(api_key=os.getenv('DEEPL_API_KEY'), source=source_lang, target=target_lang)
        return translator.translate(text)
    except Exception as e:
        return text
```

3. Добавьте в `.env`:
```
DEEPL_API_KEY=your_key_here
```

---

## Мониторинг переводов

### Проверить сколько переводов сделано:
```bash
# Подключитесь к MongoDB
mongosh mongodb://localhost:27017/test_database

# Посмотрите переводы
db.services.find({"name_en": {$exists: true}}).count()
db.cases.find({"title_en": {$exists: true}}).count()
```

### Очистить кеш переводов (если нужно перегенерировать):
```javascript
// Удалить все английские переводы
db.services.updateMany({}, {$unset: {name_en: "", description_en: "", features_en: ""}})
db.cases.updateMany({}, {$unset: {title_en: "", description_en: "", challenge_en: "", solution_en: "", results_en: ""}})
```

---

## Рекомендации

### Для малого/среднего трафика (< 10,000 посетителей/месяц):
✅ **Используйте текущую настройку** (deep-translator + Google Translate)
- Бесплатно
- Достаточно быстро
- Переводы кешируются

### Для высокого трафика или критичной системы:
⚡ **Переключитесь на Google Cloud Translation API или DeepL**
- Стабильнее
- Нет лимитов
- Лучшее качество

---

## FAQ

**Q: Нужен ли мне API ключ сейчас?**
A: Нет! Текущая версия работает без ключей.

**Q: Что если Google заблокирует мой IP?**
A: Маловероятно для обычного сайта. Но если случится - переводы из кеша продолжат работать.

**Q: Могу ли я редактировать переводы?**
A: Да! В админке можно редактировать любые поля, включая *_en поля.

**Q: Как часто происходит перевод?**
A: Только при первом запросе для нового контента. Потом используется кеш.

**Q: Могу ли я отключить автоперевод?**
A: Да, просто удалите параметр `?lang=en` из запросов и переводы не будут генерироваться.
